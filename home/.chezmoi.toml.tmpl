{{- /* Prompt-once values are persisted to ~/.config/chezmoi/chezmoi.toml.   */ -}}
{{- /* Edit that file (or `chezmoi edit-config`) to change role afterwards. */ -}}
{{- /* Git user.name / user.email come from the post-install configure-git */ -}}
{{- /* script (run_once_after_50-configure-git.sh), not chezmoi prompts.   */ -}}

{{- $role := promptChoiceOnce . "role" "Machine role"
    (list "desktop-mac" "desktop-fedora" "devcontainer" "pi") -}}

# Read the source state from the working repo, not the default
# ~/.local/share/chezmoi. Keeps bare `chezmoi apply` and `just apply` in sync.
sourceDir = "~/.dotfiles"

[data]
    role = {{ $role | quote }}

[edit]
    command = "nvim"

[diff]
    pager = "less -R"
